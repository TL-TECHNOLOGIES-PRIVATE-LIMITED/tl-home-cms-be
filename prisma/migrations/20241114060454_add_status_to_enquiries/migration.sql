BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Enquiries] ADD [status] NVARCHAR(1000) NOT NULL CONSTRAINT [Enquiries_status_df] DEFAULT 'unread';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
